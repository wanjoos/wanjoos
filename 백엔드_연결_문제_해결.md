# 🚨 백엔드 연결 문제 해결 가이드

## ❌ 현재 문제

**증상:**
- 메인 화면에 학년 선택 버튼이 표시되지 않음
- "학년을 선택하여 출석체크를 시작하세요" 메시지만 보임

**원인:**
```
1. 현재 Netlify에 업로드한 파일은 프론트엔드만 포함
2. 백엔드 서버가 없어서 /api/grades 호출이 실패
3. grades 데이터를 불러오지 못해서 학년 카드가 렌더링되지 않음
```

**API 호출 실패:**
```javascript
// 프론트엔드가 호출하는 API
GET /api/grades          // 학년 데이터 (학년별 자습공간)
GET /api/students        // 학생 목록
POST /api/attendance     // 출석 저장
GET /api/statistics      // 통계 조회
```

---

## ✅ 해결 방법 (3가지 옵션)

### 옵션 1: Mock 데이터로 테스트 (가장 빠름) ⭐

백엔드 없이 테스트할 수 있도록 하드코딩된 데이터 사용

#### 장점
- ✅ 즉시 테스트 가능
- ✅ 백엔드 설정 불필요
- ✅ UI/UX 확인 가능

#### 단점
- ❌ 출석 데이터 저장 불가
- ❌ 실제 학생 명단 사용 불가
- ❌ 통계 기능 사용 불가

#### 구현 방법
프론트엔드 코드를 수정해서 API 호출 대신 로컬 데이터 사용

---

### 옵션 2: 백엔드 서버 별도 배포 (추천) ⭐⭐⭐

백엔드를 무료 호스팅 서비스에 배포

#### 추천 서비스

**A. Render.com (추천!)**
- ✅ 무료 티어 제공
- ✅ 간단한 설정
- ✅ GitHub 연동 자동 배포
- ✅ 슬립 모드 (15분 미사용 시)

**B. Railway.app**
- ✅ 무료 티어 ($5 크레딧/월)
- ✅ 빠른 배포
- ✅ 간단한 UI

**C. Fly.io**
- ✅ 무료 티어
- ✅ 글로벌 배포

#### 장점
- ✅ 완전한 기능 사용 가능
- ✅ 데이터 저장 가능
- ✅ 무료 (소규모)

#### 단점
- ⚠️ 초기 설정 필요 (30분)
- ⚠️ 서버 관리 필요

---

### 옵션 3: Netlify Functions (서버리스) ⭐⭐

백엔드를 Netlify Functions로 변환

#### 장점
- ✅ 서버 불필요
- ✅ Netlify와 통합
- ✅ 무료 티어 충분

#### 단점
- ⚠️ 코드 변환 필요
- ⚠️ 파일 저장 제한

---

## 🎯 권장 방법: Render.com 배포

### Step 1: Render.com 계정 생성

1. https://render.com/ 접속
2. "Get Started" 클릭
3. GitHub 계정으로 로그인

### Step 2: 백엔드 배포

1. **New Web Service** 클릭
2. GitHub repository 연결: `wanjoos/wanjoos`
3. 설정:
   ```
   Name: ksa-attendance-backend
   Root Directory: backend
   Environment: Node
   Build Command: npm install
   Start Command: npm start
   ```
4. **Free** 티어 선택
5. **Create Web Service** 클릭

### Step 3: 환경 변수 설정

Render 대시보드에서:
```
Environment Variables:
PORT = 3001
NODE_ENV = production
```

### Step 4: 배포 완료 대기

- 3~5분 소요
- 배포 완료 후 URL 제공 (예: `https://ksa-attendance-backend.onrender.com`)

### Step 5: 프론트엔드에서 백엔드 URL 설정

프론트엔드 코드에서 API URL 변경:

```javascript
// frontend/src/App.jsx
const API_BASE = 'https://ksa-attendance-backend.onrender.com/api';
```

### Step 6: 프론트엔드 재빌드 및 재배포

```bash
cd /home/user/webapp/frontend
npm run build

cd /home/user/webapp
rm -rf deploy_package
mkdir deploy_package
cp -r frontend/dist/* deploy_package/
cp students.json deploy_package/

zip -r netlify_deploy_with_backend_$(date +%Y%m%d_%H%M).zip deploy_package/
```

### Step 7: Netlify 재업로드

- 새 zip 파일 다운로드
- Netlify에 업로드

---

## 🚀 빠른 테스트: Mock 데이터 버전

백엔드 없이 즉시 테스트하고 싶다면 Mock 데이터 버전을 만들어드릴 수 있습니다.

### Mock 데이터 특징
- ✅ 학년 선택 가능
- ✅ 자습공간 선택 가능
- ✅ 학생 목록 표시
- ✅ 출석 상태 변경 (로컬만)
- ❌ 데이터 저장 불가 (새로고침 시 초기화)

### 구현 방법

프론트엔드 코드를 수정해서:
1. API 호출을 Mock 함수로 대체
2. 로컬 스토리지에 임시 저장
3. students.json을 번들에 포함

---

## 📊 비교표

| 방법 | 설정 시간 | 비용 | 기능 완전성 | 유지보수 | 추천도 |
|------|----------|------|-------------|----------|--------|
| Mock 데이터 | 5분 | 무료 | ⭐ | 쉬움 | ⭐⭐ |
| Render 배포 | 30분 | 무료 | ⭐⭐⭐ | 보통 | ⭐⭐⭐ |
| Netlify Functions | 1시간 | 무료 | ⭐⭐⭐ | 어려움 | ⭐⭐ |

---

## ❓ 어떤 방법을 선택해야 하나요?

### 지금 당장 테스트만 하고 싶다면
→ **Mock 데이터 버전** (5분 완성)

### 실제로 사용하려면
→ **Render.com 백엔드 배포** (30분, 추천!)

### 장기적으로 운영하려면
→ **GitHub 자동 배포 + Render** (최고의 조합)

---

## 🎯 추천 로드맵

### 1단계: 즉시 (오늘)
- Mock 데이터 버전으로 UI 테스트
- 선생님들께 화면 미리 보여주기

### 2단계: 이번 주
- Render.com에 백엔드 배포
- 실제 학생 데이터로 테스트

### 3단계: 다음 주
- GitHub 자동 배포 설정
- 정식 운영 시작

---

## 🛠️ 지금 당장 할 수 있는 것

### 옵션 A: Mock 버전 요청
"Mock 데이터 버전으로 만들어주세요"라고 요청하시면:
- 백엔드 없이 작동하는 버전 제공
- 5분 안에 배포 가능
- UI/UX 즉시 확인

### 옵션 B: Render 배포 도움
"Render에 백엔드 배포 도와주세요"라고 요청하시면:
- 단계별 가이드 제공
- 설정 파일 생성
- 배포 완료까지 안내

---

## 📝 요약

**현재 상황:**
- ✅ 프론트엔드: 정상 배포됨
- ❌ 백엔드: 연결 안 됨
- ❌ 학년 버튼: 표시 안 됨

**원인:**
- API 호출 실패 (백엔드 없음)

**해결책:**
1. **빠른 테스트**: Mock 데이터 버전 (5분)
2. **실제 사용**: Render 배포 (30분) ⭐ 추천
3. **고급**: Netlify Functions (1시간)

**다음 단계:**
원하는 방법을 선택하시면 도와드리겠습니다!

---

**도움이 필요하면:**
- "Mock 버전 만들어줘" 
- "Render 배포 도와줘"
- "Netlify Functions로 변환해줘"

중 하나를 말씀해주세요! 🚀
